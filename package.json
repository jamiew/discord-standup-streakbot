{
  "name": "discord-standup-streakbot",
  "version": "1.0.0",
  "description": "Discord bot for tracking standup streaks",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "dev": "pnpm dlx nodemon bot.js --ext 'js,mjs,cjs' --watch '.'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "discord.js": "^14.0.0",
    "dotenv": "^16.0.0",
    "lowdb": "^1.0.0",
    "node-fetch": "2",
    "node-schedule": "^2.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0"
  }
}
